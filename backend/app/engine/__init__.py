# The trained transformer (model + tokenizer + inference) goes here.
# It should implement the same generate(GenerateRequest) -> GenerateResponse as
# services.generator.Generator; then get_generator() returns it instead of the
# PlaceholderGenerator.
